#!/bin/sh

# Run the build script
npm run build

# Check if the build was successful
if [ $? -ne 0 ]; then
  echo "Build failed. Commit aborted."
  exit 1
fi

# Add the build output to the commit
git add .

# Continue with the commit
exit 0